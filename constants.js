// File with constants for strings and other data that are "static" in my code
const host = '165.227.47.243';
const port = 50541;

const moveUp = 'w';
const moveDown = 's';
const moveLeft = 'a';
const moveRight = 'd';
const message = "p";

module.exports = {
  host,
  port,
  moveUp,
  moveDown,
  moveLeft,
  moveRight,
  message
};